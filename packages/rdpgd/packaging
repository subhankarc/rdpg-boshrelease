#!/usr/bin/env bash
set -e

package="rdpgd"
version="0.1.1"
file="${package}_${version}_linux_amd64.tar.gz"

mkdir -p ${BOSH_INSTALL_TARGET}/bin

tar zxf ${package}/${file} --strip-components=1 -C ${BOSH_INSTALL_TARGET}/bin \
  $(tar -tvf ${package}/${file} | awk '/rdpgd$/{print $NF}')

