meta:
  environment: (( merge ))
  stemcell: (( merge ))

name: (( meta.environment ))

director_uuid: (( merge ))

releases: (( merge ))

networks: (( merge ))

jobs: (( merge ))

properties: (( merge ))

compilation:
  workers: 2
  network: rdpg
  reuse_compilation_vms: true
  cloud_properties: (( merge ))

update:
  canaries: 2
  max_in_flight: 4
  canary_watch_time: 3000-120000
  update_watch_time: 3000-120000
  serial: false

resource_pools:
  - name: manager_cluster
    network: rdpg
    size: (( auto ))
    stemcell: (( meta.stemcell ))
    cloud_properties: (( merge ))

  - name: service_cluster_a
    network: rdpg
    size: (( auto ))
    stemcell: (( meta.stemcell ))
    cloud_properties: (( merge ))

  - name: errand_a
    network: rdpg
    stemcell: (( meta.stemcell ))
    cloud_properties: (( merge ))
